info:
  name: Get Cart
  type: http
  seq: 1

http:
  method: GET
  url: "{{local}}/cart"
  headers:
    - name: x-session-id
      value: guest-session-123
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
