info:
  name: Clear Cart
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{local}}/cart"
  headers:
    - name: x-session-id
      value: guest-session-123
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
