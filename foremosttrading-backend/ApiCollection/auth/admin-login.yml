info:
  name: admin-login
  type: http
  seq: 3

http:
  method: POST
  url: "{{local}}/auth/v1/admin/login"
  body:
    type: json
    data: |-
      {
        "email":"admin@example.com",
        "password":"change-me-immediately"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
